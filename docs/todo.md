# TODO (только незакрытое; сделано — удалить)

Порядок: сверху вниз. Одна задача = вертикальный срез (бэк+фронт), заканчивается
работающим состоянием + коммитом. Не начинать следующую без явной команды.

- [ ] 2b. Включить RLS (neon_authorize) на categories/transactions, policy
      user_id = auth.user_id(). Отложено при задаче 2 — требует доверия к JWKS
      Neon Auth в консоли Neon (сделать до продакшена, не блокирует 3 — app-layer
      фильтр по userId уже защищает данные).

- [ ] 4. Дашборд + графики.
      Запросы getMonthSummary/getByCategory/getByDay, круговая по категориям, столбчатая
      по дням (Recharts).
      Готово: дашборд отражает данные текущего месяца, переключение месяцев работает.

- [ ] 5. Telegram: привязка аккаунта.
      Таблицы telegram_accounts, telegram_link_tokens. Страница settings/telegram,
      генерация одноразового токена и deep-link, вебхук обрабатывает /start <token>.
      Готово: можно привязать TG-аккаунт, статус привязки виден в вебе; можно отвязать.

- [ ] 6. Telegram: добавление транзакции сообщением.
      Парсер "кофе 200", сопоставление категории, вставка транзакции, ответ бота.
      Готово: сообщение "кофе 200" в TG создаёт транзакцию, видную в вебе.

## Фаза 2 (после подтверждения MVP)
- [ ] 7. Бюджетные лимиты: таблица budgets, вкладка лимитов, индикатор превышения.
- [ ] 8. Управление категориями (создание/редактирование/иконки).
- [ ] 9. Отдельный проход анимаций по явной команде.
